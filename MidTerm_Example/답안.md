1. virtual void Start() = 0; {}, ; 다 다르다

2. 105번째 줄
    while(!isRunning){
        currentTime=high_resolution_clock::now();
        // 뒷부분 그대로
    }

3. 
if (moveUp)    y -= speed * dt;
if (moveDown)  y += speed * dt;
if (moveLeft)  x -= speed * dt;
if (moveRight) x += speed * dt;

4. while문에 !isRunning이 되어있어서 게임루프가 시작되지 않음. isRunning이 true가 되도록 수정해야함.

5. ~GameObject()
    {
        for (int i = 0; i < (int)components.size(); i++)
        {
            delete components[i];
        }
        components.clear();
    }